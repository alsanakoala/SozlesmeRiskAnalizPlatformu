spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/contractrisk
    username: serad
    password: "1234"
    driver-class-name: org.postgresql.Driver
    hikari:
      auto-commit: false        # ⬅️ LOB okuma/yazma için gerekli
  
  jpa:
    open-in-view: false
    hibernate:
      # ddl-auto: validate   # ⬅️ ÖNCEKİ: şema sabit, kolon eklemez, eksik kolon olunca patlatır
      ddl-auto: update       # ⬅️ YENİ: local/dev için. document tablosuna ocr_used kolonunu otomatik ekle
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true  # ⬅️ Postgres LOB ile uyum için faydalı

  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB

  flyway:
    enabled: true
    locations: classpath:db/migration

  # 🔇 RabbitMQ auto-config kapalı, listener'lar devre dışı
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
  rabbitmq:
    listener:
      simple:
        auto-startup: false
      direct:
        auto-startup: false

server:
  port: 8080

app:
  rules:
    path: classpath:risk/rulepack.yml
  nlp:
    # Eğer en-sent.bin / tr-sent.bin dosyaların yoksa geçici olarak boş bırakabilirsin:
    # sentenceModelEn:
    # sentenceModelTr:
    sentenceModelEn: classpath:nlp/en-sent.bin
    sentenceModelTr: classpath:nlp/tr-sent.bin
  python:
    enabled: false
    baseUrl: http://localhost:8000
  features:
    mq:
      enabled: false      # ⬅️ RabbitMQ listener ve producer devre dışı

# =====================================================
# 🧠 AI / NLP / Embedding Ayarları
# =====================================================
riskguard:
  ai:
    similarity:
      log: true           # ⬅️ AI öneri kutularında “Benzerlik Rozeti” görünür
      embedding:
        model: "sentence-transformers/all-MiniLM-L6-v2"
        minSim: 0.85

pdf:
  renderer:
    mode: puppeteer
    url: http://localhost:3005/render
    base-url: http://localhost:8080/     # HTML içindeki göreli asset kökü
    token: CHANGE_ME_SECRET

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: contract-risk-platform

resilience4j:
  timelimiter:
    instances:
      pdfRender:
        timeoutDuration: 12s
  retry:
    instances:
      pdfRender:
        maxAttempts: 3
        waitDuration: 500ms
  circuitbreaker:
    instances:
      pdfRender:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 20s
    
